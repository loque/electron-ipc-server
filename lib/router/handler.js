'use strict'

// types: middleware, callback, route

const Handler = module.exports = function Handler()
{
    this.type
    this.name
    return this
}

Handler.handle = function handle(req, res, next)
{

}
